{% extends 'layout.html' %}

{% block body %}
	<div class="ui padded stackable grid">
			<div class="four wide column"></div>
			<div class="eight wide column">
				<p>&nbsp</p>
				<p>&nbsp</p>
				<div class="ui raised segment">	
				<h1 class="ui dividing header">
				  	<i class="ui orange idea icon"></i>
				  	Suggestion
				</h1>
				{% with messages = get_flashed_messages(True) %}
		      	{% for category, message in messages %}
		      		<div class="ui fluid {{ category }} message">
		      			<i class="ui close icon"></i>
		      			<div class="ui header">{{ category }}</div>
		      			{{ message }}
		      		</div>
		      	{% endfor %}
		      {% endwith %}
				<form class="ui message form" action="{{ url_for('labs') }}" method="POST">			  
					<div class="required field">
					    <label>Long name</label>			    
					    <div class="field">
					    	<input class="ui input" type="text" name="long-name" placeholder="Example: kilogrammes" required>
					    </div>			      
					</div>
					
					<div class="required field">
					    <label>Short name</label>			    
					    <div class="field">
					    	<input class="ui input" type="text" name="short-name" placeholder="Example: KG" required>
					    </div>			      
					</div>
				
				  	<p>&nbsp</p>

					<div class="field">
				  		<button class="ui large blue button" name="btn-disburse"><i class="ui add circle icon"></i>Add unit</button>
				  	</div>

				 </form>
				</div>
			</div>
		</div>
{% endblock %}
